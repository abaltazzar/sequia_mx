% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resumen_entidad.R
\name{resumen_entidad}
\alias{resumen_entidad}
\title{Resumen de sequía por unidad geográfica y fecha}
\usage{
resumen_entidad(
  datos = sequiaMX::sequia_municipal,
  peso = c("municipios", "superficie", "poblacion"),
  nivel_geo = c("entidad", "nacional", "org_cuenca", "con_cuenca"),
  fechas = NULL
)
}
\arguments{
\item{datos}{Tabla larga, normalmente \code{sequia_municipal}.}

\item{peso}{\code{"municipios"} (conteo), \code{"superficie"} (km2) o \code{"poblacion"}
(población CONAPO del año de la fecha).}

\item{nivel_geo}{\code{"entidad"} (default), \code{"nacional"}, \code{"org_cuenca"} o
\code{"con_cuenca"}.}

\item{fechas}{Vector opcional de fechas para filtrar antes de agregar.}
}
\value{
Tibble con una fila por unidad geográfica, fecha y categoría, con
\code{n} (municipios), \code{valor} (suma del peso), \code{total} (suma del peso en la
unidad-fecha) y \code{proporcion} (\code{valor / total}). Incluye ceros explícitos
para categorías ausentes.
}
\description{
Cuenta municipios por categoría del Monitor de Sequía y calcula la
proporción, ponderada por número de municipios, superficie o población.
Es la misma lógica de la gráfica de dona, generalizada.
}
